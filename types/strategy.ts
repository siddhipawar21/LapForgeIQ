export type StrategyRecord = {
  id: string;
  track: string;
  weather: string;
  tireCompound: string;
  aggression: string;
  predictedFinish: string;
  pitStrategy: string;
  tireRecommendation: string;
  overtakeWindows: string;
  fuelStrategy: string;
  safetyCarPlan: string;
  bestLapPrediction: string;
  riskAnalysis: string;
  createdAt: string;
};